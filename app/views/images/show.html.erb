<% if @user.theme && @user.theme.property_show %>
  <%= render :inline => @user.theme.property_image, :locals => {:user => @user, :property => @property} -%>
<% else %>
  <%= render :partial => 'images/detail', :locals => {:user => @user, :property => @property, :image => @image} %>
<% end %>