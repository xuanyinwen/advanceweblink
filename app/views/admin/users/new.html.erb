<h2>User Admin: New User</h2>

<%- form_for @user, :url => admin_users_path(@user), :html => {:multipart => true} do |f| -%>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => {:f => f} -%>
  <%= f.submit "Save" %> <%= link_to 'cancel', admin_users_path %>
<% end %>